name: Analisis Valor Verde

on:
  workflow_dispatch: {}  # se lanza a mano desde la pestaña Actions

permissions:
  contents: write

jobs:
  analisis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install pandas requests numpy

      - name: Run analisis
        run: python analisis_valor_verde.py

      - name: Commit results
        run: |
          git config user.name "koncorde-bot"
          git config user.email "actions@github.com"
          git add analisis_valor_verde.md
          git diff --staged --quiet || git commit -m "Analisis valor verde [skip ci]"
          git pull --rebase || true
          git push
